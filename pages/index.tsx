import Layout from "../components/Layout/Layout";
import React, { FC } from "react";

const Home: FC = () => {
  return (
    <Layout>
      <h1>Hello, Next.JS!</h1>
    </Layout>
  );
};

export default Home;
